# Local Browser Configuration
browser.name=chrome
browser.version=latest
browser.size=1920x1080

# Execution Mode
run.remote=false

# Remote features disabled for local
enable.vnc=false
enable.video=false

# Timeout Configuration
timeout.page.load=30