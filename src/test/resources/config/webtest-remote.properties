# Remote Browser Configuration
browser.name=chrome
browser.version=latest
browser.size=1920x1080

# Execution Mode
run.remote=true
remote.host=selenoid.autotests.cloud

# Remote features enabled
enable.vnc=true
enable.video=true

# Timeout Configuration
timeout.page.load=30